# Get all descendant nodes for a given node

Get all descendant nodes for a given node

## Usage

``` r
get_descendant_nodes(node_id, node_tracker, node_dat)
```

## Arguments

- node_id:

  Node ID

- node_tracker:

  Node tracking structure

- node_dat:

  Node data

## Value

Data table of descendant nodes
