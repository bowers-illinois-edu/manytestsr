# Get parent ID from tracker

Get parent ID from tracker

## Usage

``` r
get_parent_from_tracker(tracker, node_ids)
```

## Arguments

- tracker:

  Node tracking object

- node_ids:

  Vector of node IDs to find parents for

## Value

Vector of parent node IDs (0 for root)
