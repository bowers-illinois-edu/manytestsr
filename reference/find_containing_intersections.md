# Find all intersection results containing a specific node

Find all intersection results containing a specific node

## Usage

``` r
find_containing_intersections(node_id, intersection_results)
```

## Arguments

- node_id:

  Node ID to find

- intersection_results:

  All intersection test results

## Value

List of intersection results containing the node
