# Find leaf descendants of a node

Find leaf descendants of a node

## Usage

``` r
find_leaf_descendants(node_id, leaf_nodes, tracker)
```

## Arguments

- node_id:

  Node ID

- leaf_nodes:

  Vector of leaf node IDs

- tracker:

  Node tracker object

## Value

Vector of leaf node IDs that are descendants
